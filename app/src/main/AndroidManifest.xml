<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.continuacao">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Continuação"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        android:usesCleartextTraffic="true"> <activity android:name=".MainActivity" android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>

        <activity android:name=".EstoqueActivity" />
        <activity android:name=".CarrinhoActivity" />
        <activity android:name=".VisualizarPedidoActivity" />
        <activity android:name=".CadastrarProdutoActivity" />
        <activity android:name=".ProdutoDetalheActivity" />
        <activity android:name=".FazerPedidoActivity" />
        <activity android:name=".CadastrarUsuarioActivity" />
        <activity android:name=".AutorizarPedidosActivity" />
        <activity android:name=".ConfigEstoqueActivity" />

    </application>
</manifest>