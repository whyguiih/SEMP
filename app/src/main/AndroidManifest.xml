<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SEMP"
        android:usesCleartextTraffic="true">

        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".EstoqueActivity" android:exported="false" />
        <activity android:name=".CarrinhoActivity" android:exported="false" />
        <activity android:name=".FazerPedidoActivity" android:exported="false" />
        <activity android:name=".ConfigEstoqueActivity" android:exported="false" />
        <activity android:name=".AutorizarPedidosActivity" android:exported="false" />
        <activity android:name=".CadastrarUsuarioActivity" android:exported="false" />

        <activity android:name=".ProdutoDetalheActivity" android:exported="false" />
    </application>
</manifest>